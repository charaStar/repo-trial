/* 

	Create events happen once when the instance is created.
	
	We want to give life to the rocks, so, we will tell
	them to move at a random direction.
	
	we first set it's speed to 1, so the rocks move at a slow pace.
	
	We then set it's direction to a random value from 0-360 (full circle).
	we also set the imag_ angle to a random value.
*/

speed = 1;
direction = random(360);
image_angle = random(360);







